// СГЕНЕРИРОВАНО scripts/gen-app-hosts.sh из реестра доменов (админка → «Домены», миграция 0089).
// Правки руками потеряются при следующей генерации: меняйте реестр в админке и перегенерируйте.
//
// Это адреса, ЗАШИТЫЕ в сборку: с них приложение начинает, пока не узнало от ядра свежий список
// (см. MayakHostList). Порядок = порядок реестра: сначала рабочие домены, затем резервные, в конце
// прямой IP ядра — он выручает, когда домен не резолвится или заблокирован по имени.
package org.amnezia.awg.mayak.core

object MayakHosts {
    val baked: List<String> = listOf(
        "https://api.mayakvpn.ru",
        // IP-фолбэк ядра: свой CA (network_security_config + res/raw/mayak_ca.pem), лиф-серт client-api
        // обязан иметь SAN=IP. Держим последним — только на случай, когда доменов не осталось.
        "https://138.16.128.138:8443",
    )

    // Веб-кабинет (регистрация, политика, продление) — роль cabinet того же реестра. Приложение
    // предпочитает адрес, присланный ядром (MayakHostList.cabinetUrl), этот — стартовый.
    const val bakedCabinet: String = "https://cabinet.mayakvpn.ru"
}
